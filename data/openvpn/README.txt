This is a dedicated folder that you can use to host the required
OpenVPN files (e.g., certs, keys, etc)

Since by default OPENVPN_RELATIVE_PATH is set to the script root,
to use this folder, you need to specify paths in openvpn conf file
as "data\\openvpn\\YOUR_FILE"